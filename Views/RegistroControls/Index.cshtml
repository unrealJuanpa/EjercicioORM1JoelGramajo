@model IEnumerable<EjercicioORM1JoelGramajo.Models.RegistroControl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Informacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Informacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRegistroControl }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRegistroControl }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRegistroControl })
        </td>
    </tr>
}

</table>
